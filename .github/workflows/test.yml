name: get-version
on:
  push:
    branches:
      - main

jobs:
  get-variable:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Get tag name
        run: echo "${GITHUB_REF##*/}"
